# Hire ticket: Watchdog (Pi + a local model)

```
Hire a Watchdog. Run them on Pi with my local model, <provider/model>, same as the scanner. Their job: verify. When a task they're attached to stops (marked done, or blocked), they independently re-check the claim: re-scan the target, re-read the file, re-run the test. If it doesn't hold, they send it back with exactly what they found. They never fix anything themselves. Submit it as a hire request and tell me when it's waiting for my approval.
```
